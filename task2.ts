//Задача:
//Напишите функцию на TypeScript, которая принимает два числа и возвращает их сумму.
const sum = (a: number, b: number): number => {
   return a+b;
}

console.log(sum(2,3))
console.log(sum(4,5))
